class UserAction {
  constructor(date, author, ip_address, resource, message, payload) {
    this.date = date
    this.author = author
    this.ip_address = ip_address
    this.resource = resource
    this.message = message
    this.payload = payload
  }
}

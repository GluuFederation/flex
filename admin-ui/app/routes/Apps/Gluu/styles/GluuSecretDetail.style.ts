import { makeStyles } from 'tss-react/mui'

export const useStyles = makeStyles()(() => ({
  valueCol: {
    display: 'flex',
    alignItems: 'center',
  },
  rowDefault: {
    alignItems: 'center',
    marginBottom: '0.5rem',
  },
}))
